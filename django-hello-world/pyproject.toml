[project]
name = "django-hello-world"
version = "0.1.0"
description = "A simple 12-factor Django application."
requires-python = ">=3.10"
dependencies = [
    "Django",
    "psycopg2-binary",
    "tzdata",
]
